import 'package:flutter/material.dart';

const smallRadius = Radius.circular(8);
const mediumRadius = Radius.circular(12);
const largeRadius = Radius.circular(16);

const smallBorderRadius = BorderRadius.all(smallRadius);
const mediumBorderRadius = BorderRadius.all(mediumRadius);
const largeBorderRadius = BorderRadius.all(largeRadius);
