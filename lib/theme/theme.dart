import 'package:flutter/material.dart';

const smallRadius = Radius.circular(8);
const mediumRadius = Radius.circular(12);
const largeRadius = Radius.circular(16);

final smallBorderRadius = BorderRadius.circular(8);
final mediumBorderRadius = BorderRadius.circular(12);
final largeBorderRadius = BorderRadius.circular(16);
